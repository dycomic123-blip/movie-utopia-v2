# Movie Utopia - 统一设计系统

## 📋 概述

本文档说明了如何统一主站和 Studio 的 UI 设计系统。

## 🎨 设计统一内容

### 1. 颜色系统
- **主色 (Primary)**: `rgb(139 92 246)` - Violet 紫色
- **强调色 (Accent)**: `rgb(245 158 11)` - Amber 琥珀色
- **背景色**: `rgb(13 13 13)` - 深色背景
- **卡片背景**: `rgb(24 24 27)` - Zinc-900
- **边框**: `rgba(255, 255, 255, 0.05)` - 半透明白色

### 2. 按钮样式

#### 主按钮 (Primary Button)
```css
background: linear-gradient(to right, rgb(139 92 246), rgb(168 85 247));
padding: 0.625rem 1.5rem;
border-radius: 0.5rem;
font-size: 0.875rem;
font-weight: 600;
box-shadow: 0 4px 12px rgba(139, 92, 246, 0.2);
```

#### 次要按钮 (Ghost Button)
```css
background: transparent;
color: rgb(161 161 170);
border: 1px solid rgba(255, 255, 255, 0.05);
padding: 0.5rem 1rem;
border-radius: 0.5rem;
```

#### Hover 效果
- 不透明度: 0.9
- 向上移动: translateY(-1px)
- 阴影增强: 0 4px 12px rgba(139, 92, 246, 0.4)

### 3. Tab 栏样式

```css
/* 标签容器 */
display: flex;
gap: 0.5rem;
border-bottom: 1px solid rgba(255, 255, 255, 0.05);

/* 单个标签 */
padding: 0.75rem 1.5rem;
font-size: 0.875rem;
color: rgb(161 161 170); /* 未激活 */
border-bottom: 2px solid transparent;

/* 激活状态 */
color: rgb(139 92 246);
border-bottom-color: rgb(139 92 246);
```

### 4. 用户头像

```css
/* 头像容器 */
width: 2rem;
height: 2rem;
border-radius: 9999px;
border: 1px solid rgba(255, 255, 255, 0.1);
background: rgb(39 39 42);

/* 用户名 */
font-size: 0.75rem;
font-weight: 500;
color: white;
```

### 5. 输入框样式

```css
background: rgba(255, 255, 255, 0.03);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 0.75rem;
padding: 1rem 1.25rem;
color: white;

/* Focus 状态 */
border-color: rgb(139 92 246);
box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
```

### 6. 卡片样式

```css
background: rgba(24, 24, 27, 0.8);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 1rem;
padding: 1.5rem;
backdrop-filter: blur(20px);
```

### 7. 玻璃效果

```css
background: rgba(24, 24, 27, 0.5);
backdrop-filter: blur(40px) saturate(180%);
border: 1px solid rgba(255, 255, 255, 0.05);
```

## 🔧 实现方式

### 文件结构
```
studio/
├── css/
│   ├── unified-styles.css    # 统一样式文件
│   ├── style.css             # 原有样式
│   └── animations.css        # 动画样式
├── js/
│   ├── style-unifier.js      # 自动样式统一脚本
│   ├── app.js                # 应用逻辑
│   └── ...
└── index.html                # 主页面
```

### 加载顺序
1. 原有 CSS 文件
2. **unified-styles.css** (覆盖原有样式)
3. 内联样式
4. 所有 JS 文件
5. **style-unifier.js** (最后执行，动态应用样式)

## 📝 使用统一类名

### 按钮
- `.btn-primary` - 主按钮
- `.btn-accent` - 强调按钮
- `.btn-ghost` - 幽灵按钮

### 输入
- `.input-unified` - 统一输入框样式

### 卡片
- `.card-unified` - 统一卡片样式
- `.glass-unified` - 玻璃效果卡片

### 头像
- `.user-avatar` - 用户头像容器
- `.user-name` - 用户名文本

### Tab
- `.tabs-container` - Tab 容器
- `.tab-item` - 单个 Tab
- `.tab-item.active` - 激活的 Tab

## 🎯 关键改进

### 1. 导航栏
- ✅ 移除了不必要的元素（Pricing、Showcase）
- ✅ 添加了"Back to Home"按钮
- ✅ Logo 点击返回首页
- ✅ 简化的用户区域

### 2. 按钮
- ✅ 从橙色改为紫色主题
- ✅ 统一圆角（0.5rem）
- ✅ 统一 Hover 效果
- ✅ 统一阴影系统

### 3. 布局
- ✅ 统一间距系统
- ✅ 统一最大宽度（1280px）
- ✅ 统一边距（1rem）

### 4. 动画
- ✅ 统一过渡时间（0.3s）
- ✅ 统一缓动函数（ease-out）
- ✅ 统一 Hover 效果

## 🧪 测试清单

### 视觉检查
- [ ] 导航栏与主站一致
- [ ] 按钮颜色为紫色渐变
- [ ] Tab 栏激活状态为紫色
- [ ] 用户头像样式匹配
- [ ] 输入框 Focus 状态为紫色
- [ ] 卡片背景和边框一致
- [ ] 所有 Hover 效果流畅

### 功能检查
- [ ] Logo 点击返回首页
- [ ] "Back to Home" 按钮工作
- [ ] 所有按钮可点击
- [ ] Tab 切换正常
- [ ] 输入框可输入
- [ ] 样式不影响原有功能

### 响应式检查
- [ ] 移动端布局正常
- [ ] 平板布局正常
- [ ] 桌面布局正常
- [ ] 元素不重叠
- [ ] 文字可读

## 🔄 维护说明

### 添加新按钮
1. 使用 `.btn-primary` 类
2. 或在 JS 中使用统一的内联样式
3. 确保 Hover 效果一致

### 添加新 Tab
1. 使用 `.tab-item` 类
2. 激活时添加 `.active` 类
3. 确保底部边框正确

### 自定义样式
- 在 `unified-styles.css` 中添加
- 使用 `!important` 覆盖原有样式（必要时）
- 保持与主站一致的设计原则

## 📚 参考

### 主站组件
- `SiteHeader.tsx` - 导航栏参考
- `Button.tsx` - 按钮参考  
- `VideoCard.tsx` - 卡片参考
- `globals.css` - 全局样式参考

### 颜色变量
```css
--primary: 139 92 246;
--accent: 245 158 11;
--background: 13 13 13;
--card: 24 24 27;
--border: 39 39 42;
```

## 🎉 结语

通过统一设计系统，Studio 现在看起来是主站的无缝延续，提供了一致的用户体验。
